# .dotfiles

* Use GNU stow
* Open `.bashrc` and add :
  ```bash
  ~/.bashrc-me
  ```

## Requirements

* sysstat
  ```bash
  sudo apt install sysstat
  ```
* [playerctl](https://github.com/acrisci/playerctl/releases)
